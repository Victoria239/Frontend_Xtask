/* Página de Dashboard — placeholder inicial */

import { Box, Typography } from "@mui/material";

export default function DashboardPage() {
  return (
    <Box p={3}>
      <Typography variant="h4">Dashboard — En construcción</Typography>
    </Box>
  );
}
